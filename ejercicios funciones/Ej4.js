// Función que determina si un número es par
function esPar(numero) {
  return numero % 2 === 0; // Devuelve true si el resto es 0
}

// Ejemplos de uso:
console.log(esPar(4)); // true
console.log(esPar(7)); // false
